#!/bin/sh
export PATH=/sbin:/bin:/usr/sbin:/usr/bin:/usr/local/sbin:/usr/local/bin
echo "Current working directory: '"$(pwd)"'"
docker run --rm -it -p 8000:8000 -v $(pwd):$(pwd) -w $(pwd) roneichiarandi/php:7.1-cli "php $*"
